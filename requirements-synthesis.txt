# Synthetic Data Generation Dependencies
# Installation: Install PyTorch CPU first, then these packages
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# pip install -r requirements-synthesis.txt

# Core dependencies
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0

# CTGAN for synthetic data generation
ctgan>=0.10.0

# SDV (Synthetic Data Vault) - optional, falls back to ctgan if unavailable
sdv>=1.0.0

# SDMetrics for quality evaluation
sdmetrics>=0.13.0

# RDT (Reversible Data Transforms) - dependency for SDV
rdt>=1.9.0

# Deep learning backend (install CPU version separately as shown above)
# torch>=2.0.0
